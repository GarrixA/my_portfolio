import SkillsComponent from "./components/SkillsComponent";

const SkillsPage = () => {
  return (
    <div
      className="h-screen bg-foreground flex items-center justify-center px-[10%] mx-auto"
      id="skills"
    >
      <SkillsComponent />
    </div>
  );
};

export default SkillsPage;
